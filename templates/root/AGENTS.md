# Global Agent Context

Load the unified agentic brain first:

- `~/.agents/BRAIN.md`
- `~/.agents/AGENTS.md`

These preferences apply unless the user asks otherwise or higher-priority
instructions override them.

Do not load old projects or histories unless explicitly requested.
Do not read, store or expose secrets.
